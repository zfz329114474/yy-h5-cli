'use strict';

var VERSION = require('../../package.json').version;
var CACHE_UTILS_NAME = '&&_cache_utils';
module.exports = {
  // RC,
  // DEFAULTS,
  CACHE_UTILS_NAME: CACHE_UTILS_NAME,
  VERSION: VERSION
};